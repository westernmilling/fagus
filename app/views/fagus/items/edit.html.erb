<h2>Edit Item</h2>

<p>
  <%= link_to 'Return to items', items_path %>
</p>

<%= form_for(@item) do |f| %>
  <%= error_messages(@item) %>

  <%= render :partial => 'form',
             :locals => {
               :categories => @categories,
               :form => f,
               :item => @item,
               :item_types => @item_types,
               :status_list => Fagus::Item.status.values} %>

  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>
